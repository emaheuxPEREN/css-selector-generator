# CSS Selector Generator

JavaScript object that creates unique CSS selector for given element.

**→ [Library documentation](./packages/css-selector-generator/README.md)** — installation, options, API.

## Packages

| Package                                                     | Description                    |
| ----------------------------------------------------------- | ------------------------------ |
| [css-selector-generator](./packages/css-selector-generator) | The library. Published to npm. |

## Development

```bash
npm ci
npm test
```

Releases go through the repository root, which is what produces the commit and
the tag:

```bash
npm run release -- patch
```
